{
  "name": "@aidrift/claude-code-plugin",
  "version": "0.4.4",
  "private": true,
  "description": "AiDrift Claude Code plugin — slash commands, hooks, and MCP server that surface drift state inside Claude Code sessions",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "lint": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
